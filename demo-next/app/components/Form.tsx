"use client";

import { useState } from "react";

interface ComponentForm {
  setProps: (props: any) => void;
}

const Form = (props: any) => {
  return (
    <>
      <div>Hello!</div>
    </>
  );
};

export default Form;
